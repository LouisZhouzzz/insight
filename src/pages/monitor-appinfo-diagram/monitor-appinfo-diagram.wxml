<custom-nav-bar>{{title}}</custom-nav-bar>
<status-page status="{{status}}" bindrenewevent="getDiagram">
    <view class="container">
        <ec-canvas id="mychart-dom-bar" canvas-id="mychart-bar" ec="{{ ec }}"></ec-canvas>
        <view wx:if="{{graExpText}}" class="txt-container">
            <text>{{graExpText}}</text>
        </view>
        <ec-canvas id="sta-bar" canvas-id="sta-bar" ec="{{ ec }}"></ec-canvas>
    </view>
</status-page>